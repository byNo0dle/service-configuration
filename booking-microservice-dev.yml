booking:
  yml: Booking Profile Dev Update

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/BookingDB
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true